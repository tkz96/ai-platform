extends: default

ignore: |
  .venv/
  node_modules/
  data/
  backups/
  state/
  compose.yaml
  .git/


rules:
  document-start: disable

  line-length:
    max: 180
    level: warning

  truthy: disable
